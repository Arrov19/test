"use strict";

console.log('Робит');
! function(e, t) {
    let islocalStorageWork = function() {
        try {
            localStorage.setItem('item', 'item1');
            console.log(localStorage);
            console.log(1222);
            localStorage.removeItem('item');
            return true;
        } catch (e) {
            console.log(1222);
            return false;
        }
    };

    if (islocalStorageWork) {
        console.log(11);
        if (!t.createElementNS || !t.createElementNS("http://www.w3.org/2000/svg", "svg").createSVGRect) return !0;

        var n,
            a,
            o = "localStorage" in e && null !== e.localStorage,
            r = function() {
                t.body.insertAdjacentHTML("afterbegin", a);
            },
            i = function() {
                t.body ? r() : t.addEventListener("DOMContentLoaded", r);
            };

        if (o && 1 == localStorage.getItem("inlineSVGrev") && (a = localStorage.getItem("inlineSVGdata"))) return i(), !0;
        (n = new XMLHttpRequest()).open("GET", "sprites.svg", !0), n.onload = function() {
            n.status >= 200 && n.status < 400 && (a = n.responseText, i(), o && (localStorage.setItem("inlineSVGdata", a), localStorage.setItem("inlineSVGrev", 1)));
        }, n.send();
    } else {
        console.log(12);
    }
}(window, document);